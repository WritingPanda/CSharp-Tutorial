namespace consoleapplication1
{
    // Can change implementation of the interface without
    // breaking the entire program
    interface Area
    {
        double Area(double width, double height);
    }
}
